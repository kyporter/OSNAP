import_data.sh: shell script, takes database name and directory of input files as 
arguments

import_data.py: shell script calls; takes database name and input file directory 
as arguments, puts data into database
