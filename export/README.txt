export_data.sh: takes database name to be exported from and directory name where 
files should be exported to; runs python scripts which will export data to files 
users.csv, facilities.csv, assets.csv and transfers.csv in target directory

